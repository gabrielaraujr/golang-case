services:
  postgresql:
    image: postgres:18.1-alpine
    container_name: postgresql
    restart: always
    hostname: postgresql
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./account/resources/db/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
      - ./account/resources/db/migrations:/migrations
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - platform

  localstack:
    image: localstack/localstack:latest
    container_name: localstack
    hostname: localstack
    ports:
      - "4566:4566"
    environment:
      SERVICES: sqs
    volumes:
      - ./account/resources/create-queues.sh:/etc/localstack/init/ready.d/create-queues.sh
    healthcheck:
      test: ["CMD", "awslocal", "sqs", "list-queues"]
      interval: 5s
      timeout: 10s
      retries: 5
    networks:
      - platform

  account:
    build:
      context: ./account
      dockerfile: Dockerfile
    hostname: account
    container_name: account
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      PORT: 8001
    depends_on:
      postgresql:
        condition: service_healthy
      localstack:
        condition: service_healthy
    networks:
      - platform

  risk-analysis:
    build:
      context: ./risk-analysis
      dockerfile: Dockerfile
    hostname: risk-analysis
    container_name: risk-analysis
    env_file:
      - .env
    depends_on:
      localstack:
        condition: service_healthy
    networks:
      - platform

networks:
  platform:
    driver: bridge
